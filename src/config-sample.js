'use strict';
const config = {
        database : {
            user: 'username',
            password: 'password',
            connectString : 'address'
        },
        ldap : {
            url :'url',
            bindUser : 'bindUser',
            bindPassword : 'bindPassword!'
        }
};

module.exports = config;